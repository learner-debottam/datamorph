{
  "name": "datamorph",
  "private": true,
  "version": "0.1.0",
  "devDependencies": {
    "turbo": "latest"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test"
  }
}
